# =====================================================
# Backend Authentication Server Configuration
# =====================================================

# MongoDB Connection (với authentication)
MONGODB_URI=mongodb://aeckTu13102006:1x31SfRU8dU2l@160.250.130.69:27017/aeckdb?authSource=admin

# JWT Secret for token generation (⚠️ THAY ĐỔI NGAY KHI DEPLOY)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin Key for protected endpoints (⚠️ THAY ĐỔI NGAY KHI DEPLOY)
ADMIN_KEY=your-admin-key-for-sync-endpoint

# Firebase Configuration
FIREBASE_DATABASE_URL=https://ttkt-aeck-edu-vn-default-rtdb.asia-southeast1.firebasedatabase.app

# Firebase Admin SDK Service Account (JSON format)
# Copy từ Firebase Console > Project Settings > Service Accounts > Generate new private key
FIREBASE_ADMIN_KEY={"type":"service_account","project_id":"ttkt-aeck-edu-vn",...}

# Server Configuration
PORT=3000
NODE_ENV=production

# =====================================================
# Sync Script Settings (cho GitHub Actions)
# =====================================================

DB_NAME=aeckdb
SYNC_INTERVAL_MINUTES=30
LOG_LEVEL=info

